const flashcardDOM = document.getElementById('flash-card');

const flashcardDatabase = axios.get()

let htmlData = '<div>';

htmlData += '<ul>';

htmlData += '</ul>';

htmlData += '</div>';

flashcardDOM.innerHTML = htmlData;